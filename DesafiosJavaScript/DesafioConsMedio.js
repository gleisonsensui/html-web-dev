let numeros = 0; //Variavel de entrada
let arrayBase=[]; //Array vazio


let consumo = 0;


// Fluxo para popular array
for(let i= 0;i < 2;i++){ 
  //numeros = gets();
  //arrayBase.push(numeros);
}

consumo = arrayBase[0] / arrayBase[1];

//console.log(arrayBase)
console.log(consumo.toFixed(3) + " km/l")